use crate::document::element::paragraph::Paragraph;use crate::document::element::run::Option;use crate::document::element::run::Run;mod document;fn main() {    let mut paragraph_0 = Paragraph::new();    paragraph_0.insert_run(Run::new("Hello".to_string()));    paragraph_0.insert_run(Run::new(",".to_string()));    paragraph_0.insert_run(Run::new("World".to_string()));    println!("{:#?}", paragraph_0);}